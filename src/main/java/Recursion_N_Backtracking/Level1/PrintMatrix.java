package Recursion_N_Backtracking.Level1;

public class PrintMatrix {

    public static void main(String[] args) {
        PrintMatrix s = new PrintMatrix();
        int[][] matrix = {{1,1,1},{1,0,1},{1,1,1}};
//        s.convert(matrix);
    }

    /*private void convert(int[][] matrix) {

        int m = matrix.length;
        int n = matrix[0].length;
        for (int i=0;i<matrix.length;i++) {
            for (int j=0;j< matrix[i].length;j++) {
                if (matrix[i][j] == 0) {
                    if(i-1>=0 && i+1 <= m-1 && j-1>=0 && j+1 <= n-1) {      //somewhere in the middle
                        //check all quadrants of box and make them zero

                    }
                    else if () {    //upper right

                    }
                    else if () {    //upper left

                    }
                    else if () {    //lower right

                    }
                    else if () {    //lower left

                    }
                }
            }
            System.out.println();
        }
    }*/
}
